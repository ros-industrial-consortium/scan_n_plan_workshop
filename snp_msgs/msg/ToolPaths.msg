# The ToolPaths message type represents all the raster lines (each of type
# ToolPath, each of which may consiste of several collinear but disconnected
# `segments`) in a process area.
snp_msgs/ToolPath[] tool_paths
