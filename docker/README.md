# Running the rosbridge

> Note: You must be connected to the internet to build this container

`cd <scan_n_plan_workshop>/docker`
`docker compose up --build`

That's it!

# Stopping the rosbridge
`cd <scan_n_plan_wosrkshop>/docker`
`docker compose down`
